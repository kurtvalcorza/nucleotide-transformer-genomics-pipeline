# Runtime pins for the standalone tutorial notebook (ENV2). This repository ships no installable
# package: the row is on licence HOLD and carries only the model card and the tutorial.
torch==2.14.0
torchvision==0.29.0
torchaudio==2.11.0
transformers==4.57.6
huggingface-hub==0.36.2
safetensors==0.8.0
numpy==2.5.3
